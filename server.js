const app = require("./Applications");

/** Fungsi Untuk Start Server Backend */
app.listen(3005, () => {
    console.log(`server started at port ${3005}: Restful API`);
})

/** Fungsi Untuk Start Server Frontend */